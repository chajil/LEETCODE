#include <stdio.h>
#include <stdlib.h>
#include <string.h>

char* longestCommonPrefix(char** strs, int strsSize) {
    if (strsSize == 0) {
        char* empty = (char*)malloc(1 * sizeof(char));
        empty[0] = '\0';
        return empty;
    }

    int firstLen = strlen(strs[0]);
    char* prefix = (char*)malloc((firstLen + 1) * sizeof(char));

    for (int i = 0; i < firstLen; i++) {
        char currentChar = strs[0][i];
        
        for (int j = 1; j < strsSize; j++) {
            // Stop if string end is reached or characters mismatch
            if (strs[j][i] == '\0' || strs[j][i] != currentChar) {
                prefix[i] = '\0';
                return prefix;
            }
        }
        prefix[i] = currentChar;
    }

    prefix[firstLen] = '\0';
    return prefix;
}
